package p2p

// Peer is an interface that represent the remote node
type Peer interface {
}

// Transport is anything that handle communication between nodes in the network
type Transport interface {
}
